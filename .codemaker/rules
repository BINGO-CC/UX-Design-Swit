# UX 设计交互编辑器 — 项目规则

## ⚠️ 自动保存规则（强制执行，不可跳过，不需等待用户确认）

**触发条件**：每当对项目文件做了任何修改（代码、文档、skill 等），在同一轮回复中必须**立即、连续**执行以下全部步骤，不得中断、不得等待用户反馈、不得分多轮完成：

### 步骤 1：更新 PROJECT_RESUME.md
- 更新文档版本号（递增小版本，如 v2.1 → v2.2）
- 更新"已实现功能"表格（如有新增功能）
- 更新"代码结构"行号范围（如代码行数变化较大）
- 在"版本历史"表中新增一行，记录本次改动摘要
- 如有新增数据结构或关键函数，补充到相应章节

### 步骤 2：Git 提交并推送
- 一条命令完成：`git add -A && git commit -m "描述" && git push`
- Git 路径：`"C:\Program Files\Git\cmd\git.exe"`
- 工作目录：`f:\codemake`
- commit message 格式：简短描述本次改动内容

### 步骤 3：回复用户
- 在回复末尾附上简要说明：已自动更新续接文档并推送至 GitHub
- 格式示例：`✅ 已自动更新 PROJECT_RESUME.md (v3.x) 并推送至 GitHub`

**重要**：以上三步必须在完成代码修改后的同一轮回复中全部执行完毕。绝不能只改代码就停下来等用户追问"是否推送"或"是否保存"。

## 项目上下文

- 主代码文件：`f:\codemake\ux-design-editor.html`（单文件 HTML 编辑器）
- 续接文档：`f:\codemake\PROJECT_RESUME.md`
- UX 设计 Skill：`f:\codemake\.codemaker\skills\ux-design.md`
- 项目类型：纯前端单文件应用，无构建工具，无后端依赖

## 编码规范

- 所有代码保持在单个 HTML 文件内（CSS + HTML + JS）
- 变量/函数命名沿用已有风格（驼峰命名，简短函数名）
- 新增 CSS 样式紧跟同类样式后面，添加注释分组
- 新增 JS 函数使用 `// ====== 标题 ======` 分隔区块
- 修改超过 300 行的文件时使用局部替换，不要重写整个文件
