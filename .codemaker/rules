# UX 设计交互编辑器 — 项目规则

## 自动保存规则

每次对话中对项目文件进行了修改（包括但不限于 ux-design-editor.html、skill 文件等），在完成用户最后一个需求后，必须自动执行以下操作：

1. **更新 PROJECT_RESUME.md**
   - 更新文档版本号（递增小版本，如 v2.1 → v2.2）
   - 更新"已实现功能"表格（如有新增功能）
   - 更新"代码结构"行号范围（如代码行数变化较大）
   - 在"版本历史"表中新增一行，记录本次改动摘要
   - 如有新增数据结构或关键函数，补充到相应章节

2. **确认保存**
   - 完成更新后，向用户简要说明已自动更新续接文档

## 项目上下文

- 主代码文件：`f:\codemake\ux-design-editor.html`（单文件 HTML 编辑器）
- 续接文档：`f:\codemake\PROJECT_RESUME.md`
- UX 设计 Skill：`f:\codemake\.codemaker\skills\ux-design.md`
- 项目类型：纯前端单文件应用，无构建工具，无后端依赖

## 编码规范

- 所有代码保持在单个 HTML 文件内（CSS + HTML + JS）
- 变量/函数命名沿用已有风格（驼峰命名，简短函数名）
- 新增 CSS 样式紧跟同类样式后面，添加注释分组
- 新增 JS 函数使用 `// ====== 标题 ======` 分隔区块
- 修改超过 300 行的文件时使用局部替换，不要重写整个文件
